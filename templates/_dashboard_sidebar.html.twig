<div class="sidebar d-flex flex-column flex-shrink-0 p-3 bg-light">
	<a href="{{ path('app_dashboard')}}" class="d-flex text-center w-100 link-dark text-decoration-none">
		<h4 class="w-100 fs-3">Dashboard</h4>
	</a>
	<hr>
	<div class="fw-semibold fs-5 text-start">
		Pseudo :
		{{app.user.username}}
		<br>
		E-mail :
		{{app.user.email}}
	</div>
	<hr>
	<ul class="nav nav-pills flex-column mb-auto fw-bold fs-5">
		<li class="nav-item d-flex justify-content-start">
			<a href="{{ path('app_earnings_index')}}" class="nav-link{% if app.request.pathinfo starts with '/earnings' %} active {% else %} link-dark {% endif %}" aria-current=" page">
				<i class="bi bi-wallet2 m-3"></i>
				Revenus
			</a>
		</li>
		<li class="nav-item d-flex justify-content-start">
			<a href="{{ path('app_monthly_expenses_index')}}" class="nav-link {% if app.request.pathinfo starts with '/monthly-expenses' %} active {% else %} link-dark {% endif %}">
				<i class="bi bi-cash-stack m-3"></i>Frais mensuels fixes
			</a>
		</li>
		<hr>
		<li class="nav-item d-flex justify-content-start">
			<a href="{{ path('app_occasional_spendings_index')}}" class="nav-link {% if app.request.pathinfo starts with '/occasional-spendings' %} active {% else %} link-dark {% endif %}">
				<i class="bi bi-cash-coin m-3"></i>Dépenses de loisirs
			</a>
		</li>
		<hr>
		<li class="nav-item d-flex justify-content-start">
			<a href="{{ path('app_savings_index')}}" class="nav-link{% if app.request.pathinfo starts with '/savings' %} active {% else %} link-dark {% endif %}" aria-current=" page">
				<i class="bi bi-piggy-bank m-3"></i>
				Épargner
			</a>
		</li>
		<hr>
		<li class="nav-item d-flex justify-content-start">
			<a href="{{ path('app_unexpected_incomes_index')}}" class="nav-link{% if app.request.pathinfo starts with '/unexpected-incomes' %} active {% else %} link-dark {% endif %}" aria-current=" page">
				<i class="bi bi-coin m-3"></i>
				Rentrées d'argent inattendues
			</a>
		</li>
	</ul>
	<hr>
	<div class="fw-bold fs-4 text-center">
		<a href="{{ path('app_budget_tracking')}}" class="nav-link {% if app.request.pathinfo starts with '/budget-tracking' %} active {% else %} link-dark {% endif %}">
			<i class="bi bi-bank m-3"></i>Suivre mon budget
		</a>
	</div>
	<hr>
	<div>
		<a href="{{ path("app_logout") }}" class="d-flex align-items-center link-dark text-decoration-none justify-content-between fs-4" id="dropdownUser2">
			<img src="{{asset('build/images/exit.svg')}}">
			<strong>Déconnexion</strong>
		</a>
	</div>
</div>
