<form method="post" action="{{ path('app_occasional_spendings_delete', {'id': occasional_spending.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ occasional_spending.id) }}">
    <button class="btn">Delete</button>
</form>
