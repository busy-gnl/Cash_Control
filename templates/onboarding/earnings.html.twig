{% extends 'base.html.twig' %}

{% block title %}Onboarding
{% endblock %}

{% block body %}
	<main class="container">

		<h1>Bienvenue
			{{app.user.username}}</h1>

		<h2 id="income">Ajoutez vos revenus mensuels réguliers</h2>

		<div class="d-flex justify-content-center">
			<table class="table-onboarding">
				<thead class="table-onboarding fw-bold">
					<tr>
						<th class="table-onboarding fs-5" colspan="3">Vos revenus mensuels</th>
						{# <th></th> #}
					</tr>
					<tr>
						<th class="table-onboarding">Type</th>
						<th class="table-onboarding">Libellé</th>
						<th class="table-onboarding">Montant</th>
					</tr>
				</thead>
				<tbody>
					{% for earning in app.user.earnings %}
						<tr>
							<td class="table-onboarding">{{earning.type}}
							</td>
							<td class="table-onboarding">{{earning.name|capitalize}}
							</td>
							<td class="table-onboarding">{{earning.amount}}
								€</td>
							{# <td class="table-onboarding">
																						{% include "earnings/_delete_form.html.twig" %}
																					</td> #}
						{% endfor %}
					</tr>
					<tr>
						<td class="table-onboarding fs-5 text-center" colspan="3">Total</td>
					</tr>
					<tr>
						<td class="table-onboarding fs-5 text-center" colspan="3">{{totalEarnings}}
							€ par mois</td>
					</tr>
				</tbody>
			</table>
		</div>

		<section class="section-onboarding">


			{{ form_start(earningsform) }}
			{{ form_widget(earningsform) }}
			<button class="btn btn-success crud-button">{{ button_label|default('Ajouter une source de revenu') }}</button>
			{{ form_end(earningsform) }}
		</section>

		<section class="section-onboarding">
			<h4>
				Astuce : Vous pouvez ajouter plusieurs sources de revenus les unes après les autres.
				<br>
				Vous pourrez toujours en ajouter, modifier et supprimer depuis votre Tableau de bord.
			</h4>
		</section>

		<section class="section-onboarding">
			<h4>
				Quand vous aurez terminé de remplir ce questionnaire, cliquez sur le bouton "Suivant" ci-dessous pour accéder à l'étape suivante.
			</h4>
			<a href="{{ path('app_onboarding_expenses')}}" class="btn btn-dark">Suivant</a>
		</section>
	</main>

{% endblock %}
