<form method="post" action="{{ path('app_monthly_expenses_delete', {'id': monthly_expense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monthly_expense.id) }}">
	<button class="btn">
		<i class="bi bi-trash3-fill"></i>
	</button>
</form>
