{% extends 'base.html.twig' %}

{% block title %}Earnings{% endblock %}

{% block body %}
    <h1>Earnings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ earning.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ earning.name }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ earning.amount }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ earning.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_earnings_index') }}">back to list</a>

    <a href="{{ path('app_earnings_edit', {'id': earning.id}) }}">edit</a>

    {{ include('earnings/_delete_form.html.twig') }}
{% endblock %}
