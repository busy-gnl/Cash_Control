{% extends 'base.html.twig' %}

{% block title %}UnexpectedIncomes index{% endblock %}

{% block body %}
    <h1>UnexpectedIncomes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Amount</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unexpected_income in unexpected_incomes %}
            <tr>
                <td>{{ unexpected_income.id }}</td>
                <td>{{ unexpected_income.name }}</td>
                <td>{{ unexpected_income.amount }}</td>
                <td>
                    <a href="{{ path('app_unexpected_incomes_show', {'id': unexpected_income.id}) }}">show</a>
                    <a href="{{ path('app_unexpected_incomes_edit', {'id': unexpected_income.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_unexpected_incomes_new') }}">Create new</a>
{% endblock %}
