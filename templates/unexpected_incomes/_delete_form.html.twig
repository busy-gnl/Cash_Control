<form method="post" action="{{ path('app_unexpected_incomes_delete', {'id': unexpected_income.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unexpected_income.id) }}">
	<button class="btn">
		<i class="bi bi-trash3-fill"></i>
	</button>
</form>
